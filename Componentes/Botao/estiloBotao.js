import {StyleSheet} from 'react-native';

const estiloBotao= StyleSheet.create({
  confirmacao:{
      color: 'white',
      fontSize:20,
      backgroundColor: 'rgba(0,126,61,0.3)',
      borderRadius:10,
      borderColor: 'white',
        borderWidth: 2,
  },
  transparente:{
    fontSize: 20,
    backgroundColor: 'rgba(0,0,0,0.0)',
    color: 'white',
    borderColor: 'white',
    borderWidth: 2,
    borderRadius:10,
  }
});

export default estiloBotao;